describe("backend API test landing zones", () => {
  it.skip("Auth endpoints validate signup, login, logout, and current user");
  it.skip("GitHub endpoints enforce OAuth and repository permissions");
  it.skip("Project endpoints isolate projects by authenticated user");
  it.skip("Package endpoints support review status updates and exports");
  it.skip("Team endpoints enforce membership permissions");
});
