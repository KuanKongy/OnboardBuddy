describe("analysis pipeline test landing zones", () => {
  it.skip("Repo inventory detects TypeScript project shape and key configs");
  it.skip("Privacy filtering excludes .env files, ignored paths, and secret-like content");
  it.skip("AST extraction records files, symbols, imports, exports, and calls");
  it.skip("AST extraction assigns stable symbol keys and body/signature hashes");
  it.skip("Workflow extraction discovers entrypoint-to-side-effect paths");
  it.skip("Code evidence model persists nodes and edges for a completed snapshot");
  it.skip("Generated sections persist source receipts and generation context for regeneration");
});
