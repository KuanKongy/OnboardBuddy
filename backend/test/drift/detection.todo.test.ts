describe("drift detection test landing zones", () => {
  it.skip("Changed file hashes are detected between snapshots");
  it.skip("Changed symbol hashes identify impacted workflow steps");
  it.skip("Linked package sections are marked stale after impacted workflow changes");
  it.skip("Linked repo docs are marked stale after referenced code changes");
  it.skip("Unchanged sections are not flagged");
});
