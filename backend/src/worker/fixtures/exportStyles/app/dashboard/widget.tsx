/**
 * Negative case: a colocated component under `app/`. Only the reserved
 * `page`/`layout` filenames are routes — everything else here is a component.
 */
export default function Widget() {
  return 'widget';
}
