/**
 * A page that lives nowhere conventional — no `pages/`, `views/`, `screens/`,
 * `app/` or `routes/` in its path. The directory heuristic cannot see it; only
 * the router config naming it makes it a route.
 */
export function BillingScreen() {
  return 'billing-screen';
}
