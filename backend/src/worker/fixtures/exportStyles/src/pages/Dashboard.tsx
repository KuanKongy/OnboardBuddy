/**
 * The inline-modifier style, which `isExported()` always understood. Kept as
 * the regression guard: reconciliation must not change this case.
 */
export default function Dashboard() {
  return 'dashboard';
}
